import styled from "styled-components";

const Container = styled.div`
  display: flex;
  align-items: center;
`;

const CounterValue = styled.input`
  border: none;
  outline: 0;
  width: 50px;
  text-align: center;
  font-size: 16px;
  font-weight: 600;
`;

export { Container, CounterValue };
